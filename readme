Установка и запуск

sudo npm install -g create-react-app
create-react-app <application-name>
cd <application-name>

после обновления с github если додавлены новые папки:
sudo npm install
npm audit fix

npm start
